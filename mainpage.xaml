<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WaifuDownloaderWindows.mainpage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WaifuDownloaderWindows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

        <Grid   x:Name="rootPanel"
>
            <Grid.RowDefinitions>
                <!-- Title Bar -->
                <RowDefinition Height="Auto" />
                <!-- App Content -->
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TitleBar x:Name="AppTitleBar"
              Title="Waifu Downloader" Grid.Row="0">
                <Grid ColumnSpacing="8"
>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />

                    </Grid.ColumnDefinitions>
                    <Button x:Name="RefreshButton" ToolTipService.ToolTip="Refresh" Grid.Column="0" Click="RefreshButton_Click" >
                        <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                    </Button>
                    <ProgressRing x:Name="Progress"  Grid.Column="1" IsActive="True" />
                <ToggleSwitch x:Name="AutoReloadSwitch" OffContent="Auto reload" OnContent="Auto reload" Grid.Column="2" Toggled="AutoReloadSwitch_Toggled"/>
                <Button x:Name="DownloadButton" ToolTipService.ToolTip="Download" Grid.Column="4" Click="DownloadButton_Click">
                        <FontIcon Glyph="&#xE896;" FontSize="16"/>
                    </Button>

                </Grid>
            </TitleBar>

            <Image x:Name="Display" Grid.Row="1" Source="null" ImageOpened="Display_ImageOpened"  />


        </Grid>

</Page>
